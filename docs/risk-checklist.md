# Risk checklist 0.1.0

Заполнен в шаге 3 задания «Релизный пакет». Для каждого пункта — короткий ответ: галочка с подтверждением или отметка непройденного пункта с планом исправления. Flag-пункты, на которые ответ «да», перенесены в секцию «Внимание» PR-описания.

Дата: 2026-05-07
Автор:
Базируется на ветке: `feat/release-package`

## Безопасность

- [x] В коде нет жёстко заданных ключей, токенов, паролей. Ключ приходит из `import.meta.env.VITE_API_KEY`, в исходниках значений нет.
- [x] API-ключи читаются из переменных окружения (`VITE_API_KEY` в `.env`, не в репозитории). `.env` закрыт `.gitignore`, в репозитории только `.env.example` с пустым значением.
- [x] Нет зависимостей с известными уязвимостями (`npm audit`). Прогон чистый: 0 уязвимостей.
- [x] MCP-серверы имеют минимально необходимые разрешения (approve flow настроен). Playwright MCP: чтение автоматически, меняющие состояние — подтверждение, `browser_evaluate` и `browser_run_code` — подтверждение обязательно.

## Устойчивость проекта

- [x] API-клиент обрабатывает таймаут и ошибки сервера, фолбэк на кеш работает. Таймаут 5 с через `AbortSignal.timeout`, до 3 повторов на `NETWORK`, `TIMEOUT`, `SERVER`, дальше — данные из `products_cache`.
- [x] Интерфейс показывает состояния `loading`, `retrying`, `fallback` и `failed`. Все четыре ветки есть в `CatalogPage.jsx`.

## Изменения, требующие внимания

Если ответ «да» — добавьте в PR-описание секцию «Внимание» с объяснением.

- [x] Добавлена новая зависимость в `package.json` (указать причину). **Да:** `husky` — чтобы `pre-push` хук с AI-ревью лежал в репозитории и ставился у всех через `npm install`. Перенесено в секцию «Внимание» пулреквеста.
- [ ] Изменён формат хранения в localStorage (`appliedPromo`, `products_cache`). Ключи и структура значений не менялись.
- [ ] Появились новые переменные окружения (отражено в `.env.example`?). Новых переменных нет.

## Выводы

Релиз готов к слиянию: шесть пунктов пройдены, `npm audit` чистый, устойчивость API-клиента и четыре состояния интерфейса проверены вручную по сценариям из контракта. Сработал один флаг — новая зависимость `husky`, причина объяснена в секции «Внимание» пулреквеста. Known issue один: риск R8 (поставщик отдаёт HTML вместо JSON) закрыт в реестре проверкой `content-type`, а в коде такой проверки нет. Практических последствий сейчас нет — невалидный ответ ловит `try/catch` вокруг разбора и переводит в `INVALID_RESPONSE`, так что фолбэк на кеш срабатывает; различить в логе «сломанный JSON» и «страницу обслуживания» пока нельзя. Проверку `content-type` переносим в следующий релиз.
